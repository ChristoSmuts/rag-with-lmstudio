---
import Layout from "../../layouts/Layout.astro";
import { ProjectWorkspace } from "../../components/ProjectWorkspace";

const { id } = Astro.params;
if (!id) {
  return Astro.redirect("/");
}
---

<Layout>
  <ProjectWorkspace client:load projectId={id} />
</Layout>
